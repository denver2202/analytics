# Быстрый старт

## ✅ Проверка системы

Базовые тесты пройдены! Все модули импортируются корректно.

### Текущий статус:
- ✓ Все зависимости установлены
- ✓ Модули импортируются
- ✓ Парсер создается
- ✓ Trends collector создается
- ⚠️ База данных: требуется настройка DATABASE_URL и применение миграций

## 🚀 Запуск интерфейсов

### 1. Streamlit интерфейс (Рекомендуется)

```bash
streamlit run app_streamlit.py
```

Или через python:
```bash
python3 -m streamlit run app_streamlit.py
```

Откроется в браузере на `http://localhost:8501`

### 2. Тестирование без интерфейса

**Быстрый тест (без БД):**
```bash
python3 -m src.scripts.quick_test
```

**Полный тест (требует БД):**
```bash
# 1. Настройте .env файл с DATABASE_URL
# 2. Примените миграции
alembic upgrade head

# 3. Запустите тест
python3 -m src.scripts.test_pipeline
```

### 3. Flask API

```bash
python3 src/app.py
```

API будет на `http://localhost:5000`

## 📝 Настройка базы данных

1. Создайте `.env` файл:
```env
DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/dbname
SCRAPE_BASE_URL=https://www.jsc-niir.ru/produkciya-2/
REQUESTS_TIMEOUT=15
REQUESTS_SLEEP_BETWEEN=1.2
USER_AGENT=demand-forecast-bot/1.0
```

2. Примените миграции:
```bash
alembic upgrade head
```

3. Проверьте подключение:
```bash
python3 -m src.scripts.test_pipeline
```

## 🔍 Что можно проверить сейчас

Даже без БД можно проверить:
- ✓ Импорт всех модулей
- ✓ Создание парсера
- ✓ Создание trends collector
- ✓ Запуск Streamlit интерфейса (покажет статус БД)

С Streamlit интерфейсом можно:
- Посмотреть структуру данных
- Попробовать парсинг (если настроен DATABASE_URL)
- Протестировать сбор трендов

