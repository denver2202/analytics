# Интерфейсы для анализа спроса

## Варианты интерфейсов

### 1. Streamlit (Рекомендуется) ⭐

**Преимущества:**

- ✅ Быстрая разработка (уже готов)
- ✅ Отлично подходит для аналитики и визуализации
- ✅ Встроенные графики (Plotly)
- ✅ Интерактивные виджеты
- ✅ Не требует знания веб-разработки

**Недостатки:**

- Менее гибкий для кастомизации UI
- Один поток обработки (но достаточно для аналитики)

### 2. Flask API + React/HTML

**Преимущества:**

- ✅ Полный контроль над интерфейсом
- ✅ Можно создать красивый современный UI
- ✅ Подходит для продакшена

**Недостатки:**

- Требует больше времени на разработку фронтенда
- Нужны знания веб-разработки

## Быстрый старт

### Вариант 1: Тестирование без интерфейса (CLI)

```bash
# Тестовый скрипт для проверки всех компонентов
python -m src.scripts.test_pipeline

# Полный цикл анализа
python -m src.scripts.run_analysis
```

### Вариант 2: Streamlit интерфейс

```bash
# Установить зависимости
pip install streamlit plotly

# Запустить интерфейс
streamlit run app_streamlit.py
```

Интерфейс откроется в браузере на `http://localhost:8501`

**Возможности Streamlit интерфейса:**

- 📈 Дашборд со статистикой
- 🛒 Просмотр и фильтрация товаров
- 📊 Анализ трендов с графиками
- 🤖 Обучение и загрузка модели
- 🔮 Генерация прогнозов и рекомендаций
- ⚙️ Управление данными (парсинг, сбор трендов)

### Вариант 3: Flask API

```bash
# Запустить API сервер
python src/app.py
```

API будет доступен на `http://localhost:5000`

**Endpoints:**

- `GET /api/products` - список товаров
- `GET /api/forecasts` - прогнозы спроса
- `GET /api/recommendations/tread-pattern` - рекомендации по протекторам
- `GET /api/analytics/demand-by-pattern` - аналитика по типам протектора
- `GET /api/analytics/trends/<keyword>` - данные тренда

## Рекомендация

Для быстрого старта и демонстрации **используйте Streamlit** - он уже полностью готов и включает все необходимые функции.

Для продакшена можно позже добавить Flask API с React фронтендом.

## Структура файлов

```
analytics/
├── app_streamlit.py          # Streamlit интерфейс ⭐
├── src/
│   ├── app.py                # Flask приложение
│   ├── scripts/
│   │   ├── test_pipeline.py  # Тестовый скрипт
│   │   └── run_analysis.py   # Полный цикл анализа
│   └── routes/
│       └── forecast_routes.py  # API для прогнозов
└── README_INTERFACE.md       # Этот файл

```
